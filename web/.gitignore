# config.js is committed (anon key only; Supabase RLS protects data).
# See web/README.md "Option 1". If the project ever goes private, switch
# to Option 2 (env-var injection) and re-add config.js here.
.vercel
node_modules/
