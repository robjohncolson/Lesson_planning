# Install screenshots

INSTALL.md references PNG screenshots at this path. They are placeholders —
a human needs to capture them on a real Windows install and drop them in:

- `1_git_bash.png` — Git Bash window open
- `2_python_path_checkbox.png` — Python installer with "Add Python to PATH" circled
- `3_miktex_on_the_fly.png` — MiKTeX installer asking about on-the-fly package installs
- `6_console_home.png` — Teacher Console home page (lesson grid left, detail right)

Until captured, the images render as broken links in INSTALL.md; the
text instructions are complete on their own.
